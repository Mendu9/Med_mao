# MAO — Multi-Agent Orchestrator (HuggingFace Spaces)

# grpcio/grpcio-tools: pin to versions with pre-built cp313 manylinux wheels on PyPI
# Must be listed FIRST so pip resolves and installs wheels before building anything else
grpcio==1.62.2
# grpcio-tools: must be <=1.62.x — versions >=1.63 require protobuf>=5 which conflicts with streamlit==1.35.0 (protobuf<4)
grpcio-tools==1.62.2
# protobuf<4 required by streamlit==1.35.0 and compatible with grpcio 1.62.x
protobuf>=3.20,<4
setuptools>=70.0.0

# --- Core orchestration ---
langgraph>=0.2.0,<0.3.0
langchain-core>=0.3.0,<0.4.0

# --- LLM (Groq — cloud, no local GPU needed) ---
groq>=0.9.0
langchain-groq>=0.3.0
langchain-huggingface>=0.1.0

# --- Memory ---
mem0ai>=0.1.0

# --- Vector store ---
qdrant-client>=1.9.0,<2.0.0

# --- Embeddings ---
sentence-transformers>=3.0.0
transformers>=4.40.0,<4.47.0

# --- Graph ---
networkx>=3.3,<4.0

# --- Reranker ---
FlagEmbedding>=1.2.0

# --- API ---
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0,<3.0.0

# --- Tools ---
duckduckgo-search>=6.1.0
wikipedia-api>=0.6.0

# --- Config / HTTP ---
requests>=2.32.0
python-dotenv>=1.0.0
numpy>=1.26.0
Pillow>=10.3.0

# --- PDF parsing ---
pypdf>=4.0.0

# --- Streamlit UI ---
streamlit>=1.35.0
plotly>=5.18.0
pyvis>=0.3.1

# --- Hallucination detection ---
ragas>=0.3.0,<0.4.0

# --- Observability ---
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=6.0.0
