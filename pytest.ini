[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = --import-mode=importlib -m "not slow"
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    integration: marks tests requiring external services
    e2e: marks end-to-end tests requiring a running server
