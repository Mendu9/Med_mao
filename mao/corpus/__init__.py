"""Corpus V1 construction: provenance-verified document artifacts.

This package owns the *document-level* corpus. Chunking, embedding and index
construction are downstream and deliberately not represented here — Phase 2
benchmarks chunking on Corpus V1 rather than inheriting the legacy layout.
"""
