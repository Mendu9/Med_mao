# MAO — HuggingFace Spaces requirements
# Generated from requirements.txt — packages not needed or problematic on HF free tier are commented out.
#
# Key differences from requirements.txt:
#   - tensorflow / tf-keras commented out (USE_TF=0 is set in app.py; avoids 2GB+ install + import hang)
#   - gradio commented out (UI is Streamlit-only; app/frontend.py is abandoned)
#   - prometheus-fastapi-instrumentator commented out (FastAPI server not running on HF Spaces)
#   - openai-whisper commented out (requires ffmpeg system package; MRI tab can be gated)
#   - opencv-python → opencv-python-headless (no GUI deps needed in headless container)
#   - All other packages are Linux-compatible and included as-is

# --- Core orchestration ---
langgraph>=0.2.0,<0.3.0
langchain-core>=0.3.0,<0.4.0

# --- LLM (Groq — cloud, no local GPU needed) ---
groq>=0.9.0
langchain-groq>=0.3.0
langchain-huggingface>=0.1.0

# --- Memory ---
mem0ai>=0.1.0

# --- Vector store ---
qdrant-client>=1.9.0  # cloud mirror for HF Spaces deployment

# --- Embeddings (local sentence-transformers, works on HF Spaces) ---
sentence-transformers>=3.0.0
# FlagEmbedding 1.3.x: transformers<4.47 required
transformers>=4.40.0,<4.47.0

# --- Graph ---
networkx>=3.3,<4.0

# --- NLP / NER ---
spacy>=3.7,<4.0
# After install: python -m spacy download en_core_web_sm
scispacy>=0.5.4
pronto>=2.5.0
nxontology>=0.5.0
biopython>=1.83
rank-bm25>=0.2.2
nltk>=3.8.0

# --- Reranker ---
FlagEmbedding>=1.2.0

# --- API ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0,<3.0.0

# --- Database (optional — metrics only) ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# --- Tools ---
duckduckgo-search>=6.1.0
wikipedia-api>=0.6.0
numexpr>=2.10.0

# --- Multimodal ---
# openai-whisper>=20231117  # requires ffmpeg system package; omit unless Dockerfile installs ffmpeg
Pillow>=10.3.0

# --- MRI prediction ---
# tensorflow>=2.16.0        # USE_TF=0 set in app.py — skip 2GB+ install to avoid import hang
# tf-keras>=2.16.0          # depends on tensorflow above
opencv-python-headless>=4.9.0  # headless: no Qt/GUI deps needed in container
numpy>=1.26.0

# --- Config / HTTP ---
requests>=2.32.0
python-dotenv>=1.0.0

# --- PDF parsing ---
pypdf>=4.0.0

# --- Data ingestion ---
datasets>=2.20.0
tqdm>=4.66

# --- Gradio UI (DISABLED — UI is Streamlit-only on HF Spaces) ---
# gradio>=5.29.0

# --- Streamlit UI ---
streamlit>=1.35.0
plotly>=5.18.0
pyvis>=0.3.1

# --- Hallucination detection ---
ragas>=0.3.0,<0.4.0

# --- Observability ---
prometheus-client>=0.20.0
# prometheus-fastapi-instrumentator>=6.0.0  # FastAPI not running standalone on HF Spaces

# --- Dev / testing ---
pytest>=8.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
